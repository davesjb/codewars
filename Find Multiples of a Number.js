function findMultiples(integer, limit) {
  let multiples = [];
  
  console.log(limit);
  
//   i= i + integer is equivalent to i += integer
  for(let i = integer; i <= limit; i= i + integer){
        
    
    multiples.push(i)
    console.log(integer);
    console.log(multiples);
    
  }